{
  "name": "myth",
  "productName": "Myth",
  "version": "0.1.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron index.js",
    "bMac": "electron-packager . --overwrite --platform=darwin --arch=x64 --prune=true --out=release-builds",
    "bWin": "electron-packager . myth --overwrite --asar=true --platform=win32 --arch=ia32 --prune=true --out=release-builds --version-string.CompanyName=CE --version-string.FileDescription=CE --version-string.ProductName=\"Myth\"",
    "bLin": "electron-packager . myth --overwrite --asar=true --platform=linux --arch=x64 --prune=true --out=release-builds"
  
  },
  "author": "fdstudio",
  "license": "MIT",
  "description": "",
  "dependencies": {
    "electron": "^1.8.2"
  },
  "devDependencies": {
    "electron-packager": "^11.0.1"
  }
}
